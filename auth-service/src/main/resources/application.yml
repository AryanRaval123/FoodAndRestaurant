server:
  port: 8081

spring:
  application:
    name: auth-service

  config:
    import: configserver:http://localhost:8888

jwt:
  secret: ThisIsMySecretKeyForJWTTokenGenerationMakeItLongEnough123456
  expiration: 86400000   # 24 hours in ms

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


